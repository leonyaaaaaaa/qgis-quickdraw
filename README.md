# qgis-quickdraw
quickdraw Tools is a QGIS plugin that makes on-the-fly digitizing fast. It adds a toolbar of simple drawing tools — circle, rectangle, line, polygon, point (with an optional X/Y or DMS coordinate entry) and buffer — and saves what you draw straight into memory layers, without the usual layer-creation dialogs getting in the way.
